---
target: dashboard Interea (app/page.tsx)
total_score: 20
max_score: 40
na_heuristics: 
p0_count: 2
p1_count: 2
timestamp: 2026-08-30T05-59-04Z
slug: app-page-tsx
---
Method: dual-agent (A: revisão de design · B: detector e evidência de browser)

## Nota de saúde do design — 20/40 (Aceitável, no piso da faixa)

| # | Heurística | Nota | Problema central |
|---|---|---|---|
| 1 | Visibilidade do estado | 3 | Três janelas de tempo na mesma linha sem controle comum |
| 2 | Linguagem do mundo real | 2 | `13 pts`, `prioridade 85`, `severidade`, `limiar` para quem não tem vocabulário de analista |
| 3 | Controle e liberdade | 1 | Zero navegação abaixo de 768px; `/resumo`, `/operacao`, `/temas` sem nenhum link |
| 4 | Consistência | 2 | O número grande do card muda de unidade a cada card |
| 5 | Prevenção de erro | 2 | Card promete "5 rascunhos prontos" e não mostra nenhum |
| 6 | Reconhecer, não lembrar | 2 | `13 pts` exige escala que nunca aparece |
| 7 | Flexibilidade | 1 | Um `<button>` no produto inteiro; zero `onClick` |
| 8 | Estético e minimalista | 2 | `/` com 438 palavras, ~30% metodologia |
| 9 | Recuperação de erro | 2 | Estado vazio de filtro sem link para limpar |
| 10 | Ajuda e documentação | 3 | App feito de documentação, menos nos 4 termos que precisam |

## Especificidade

Aprovado decisivamente — nav `Decidir/Entender/Conferir`, taxonomia fechada de seis movimentos, recusa estrutural a culpar atendente, vocabulário de escola brasileira. Mas a autoria está mirada no comprador cético, não na diretora cansada.

Detector: 0 achados em 24 arquivos (verificado com --no-config; genuíno mas estreito — o motor não avalia Tailwind). Sem overlay visual (Chrome headless não permite injeção mutável); evidência por CDP.

## Problemas prioritários

**[P0] Nada é acionável.** 1 `<button>`, 0 `onClick`. 14+ rascunhos prontos sem botão de copiar/aprovar/resolver. `/conversa/2` imprime o fluxo pretendido sem oferecer os controles. → `/impeccable shape`

**[P0] Sem navegação no celular.** `aside` com `display:none` nas 5 rotas, 0 de 7 links visíveis; marca mobile é `<span>`. `/operacao` e `/temas` com zero elementos tocáveis na primeira tela. → `/impeccable adapt`

**[P1] Metodologia dentro da ferramenta do cliente.** ~30% das palavras de `/`; dois de três painéis de `/resumo`; "a diretora não compra o que a secretária vai odiar" exibido para a diretora; "texto de base, ainda não gerado pelo modelo" no texto que ela vai publicar. → `/impeccable distill`

**[P1] Números grandes ordenam a lista ao contrário.** `147 conversas` domina o card sem dinheiro; o card de R$41.033 mostra `5`. Mais `pts`/`prioridade` sem escala, e contradição entre "limiar, não ranking" e `/pessoas` ranqueando 01…08 com nota. → `/impeccable clarify`

**[P2] Contraste e tipo minúsculo.** `text-muted-foreground/70` em card = 4.02:1 a 11px (legenda de todo KPI). `RECUPERAR` = 3.73:1 a 10px no card de maior valor. Reset global `* { outline-ring/50 }` deixa todo foco em 1.87:1. Gráfico: s1 vs s2 = 1.07:1 de luminância, sem canal redundante. → `/impeccable polish`

## Menores

Salto h1→h3 em `/decisoes`; tooltips de gráfico sem equivalente de teclado (tabIndex 0 em todos os SVGs); chips de `/conversas` descartam a busca; corte de 60 conversas sem paginação; espaço morto nas grades de 2 colunas; componentes shadcn instalados e sem uso; tile "1.451 mensagens lidas" ocupa a posição mais lida com o número menos útil.

## Perguntas

E se `/resumo` fosse `/`? Qual é o único botão? Como é a segunda depois que ela age? E se não houvesse linha de KPI? `147 conversas` é maior que `R$ 41.033` — a lista de decisões faz com tipo o que vocês criticam a escola por fazer com fila.
